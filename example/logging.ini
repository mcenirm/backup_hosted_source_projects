[formatters]
keys = basic

[formatter_basic]
format = %(levelname)s:%(name)s:%(message)s
datefmt =
class = logging.Formatter

[handlers]
keys = basic

[handler_basic]
class = logging.StreamHandler
args = (sys.stderr,)

[loggers]
keys = root

[logger_root]
level = DEBUG
handlers = basic
